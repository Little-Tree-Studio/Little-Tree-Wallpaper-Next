# 安全政策

<p align="center">
  <a href="./SECURITY.md">English</a> | 简体中文
</p>

## 支持的版本

小树壁纸 Next 2.0 处于 Beta 阶段，每个渠道仅最新发布版本会获得安全修复。

| 版本 / 渠道 | 是否支持 |
| --- | --- |
| 最新的 `2.0.x-beta` 发布版 | 支持 |
| 更早的 beta 发布版 | 不支持——请升级 |
| 1.x（上一代） | 不支持——独立的项目生命周期 |

## 报告漏洞

**请勿以公开 Issue 的形式报告安全问题。**

请使用 GitHub 的私密漏洞报告渠道：打开仓库的 **Security** 标签页并选择 "Report a vulnerability"；如无法使用该表单，也可直接通过邮箱 **studio@zsxiaoshu.cn** 联系维护者。

通过邮件报告时，请使用可识别的主题（如 `[security] 收藏包导入路径穿越`），并对敏感细节做好加密；如需加密可向我们索取 PGP 公钥。

报告时请附上：

- 受影响的版本/commit 与构建渠道。
- 平台（操作系统版本）与应用安装方式。
- 最小复现：构造的文件、命令序列或网络抓包。
- 你对影响面的评估与已知的缓解方式。
- 是否希望在公开致谢中署名。

我们会在大约 5 个工作日内确认收到。修复与披露的时间线将与报告人协调；在修复版本发布前，请对发现内容保密。除非你希望匿名，我们乐于在公告中致谢。

## 范围

以下情况被视为本应用的安全问题：

- **本地 API 保护**：绕过或削弱仅回环绑定、每次启动生成的访问令牌、预览 URL 鉴权；来自其他本地进程或网站的跨源访问。
- **路径穿越 / 任意文件写入**：通过构造的收藏包、`.ltwp` 工程、主题包、壁纸源包、商店下载或任何导入导出路径。
- **非用户意图的代码执行**：不可信输入处理器（`.ltp` 插件包、`.ltauto` 自动化、`.lttheme` 主题、商店元数据）中的解析缺陷，在用户确认信任之前即导致代码执行。
- **权限提升或隔离失效声明**：插件实际能力与文档不符属于文档缺陷；但 `backend/plugins/` 内部校验假设失效（如压缩包条目校验、清单校验被绕过）属于漏洞。
- **密钥处理**：API Key、令牌或个人配置泄露到文档所述存储位置之外。

### 明确不在范围内

- **用户主动选择的恶意扩展。** 插件是经用户确认后加载的可信进程内 Python 代码，不具备沙箱；该信任模型已在 [`docs/PLUGINS.md`](docs/PLUGINS.md) 与 README 注意事项中说明。安装已知恶意插件不构成应用漏洞——但允许恶意包绕过上述校验的解析或验证缺陷属于漏洞。
- **Windows 破坏未公开 API。** 动态壁纸依赖 Explorer WorkerW 行为，Microsoft 可能随时调整；由此产生的不稳定属于兼容性问题，而非安全缺陷。
- **第三方服务的可用性或内容**，包括壁纸/图片来源与资源商店的远端内容。
- **社会工程**：在应用已展示全部警告的前提下，诱导用户安装恶意主题/插件/自动化。
- 针对已停止维护版本、且未在最新发布版上复现的报告。

## 安全设计说明

供评估报告时参考：

- 后端 FastAPI 服务仅绑定 `127.0.0.1` 的随机端口；每次启动生成新的访问令牌，前端及媒体/预览 URL 均需要该令牌。
- 插件是经用户显式确认后进程内执行的 Python 模块，**没有**沙箱。包校验可阻止目录穿越、原生可执行文件与超限负载，但无法证明代码安全。
- 高级自动化节点可以执行任意程序、读写文件、打开 URL 并触发电源操作；导入 `.ltauto` 文件时有确认流程。
- 主题的自定义 CSS 全局生效；主题内的远程字体/媒体可能发起网络请求。

## 公开披露

修复发布后，我们会发布 GitHub Security Advisory，注明受影响版本、修复版本与致谢。修复优先落在最新的 beta/stable 分支；项目整体进入稳定期前不对旧版本做回移移植。
